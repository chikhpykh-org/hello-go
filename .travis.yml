language: go

go:
  - 1.7
  
install:
  - go get github.com/tcnksm/ghr
  
script: go build -ldflags="-s -w" -v -o test && ls -alF && ghr -t $secNval -u chikhpykh-org -r hello-go --replace v0.1.0 ./test
